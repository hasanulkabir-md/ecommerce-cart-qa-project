name: CI - E-commerce Cart QA Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 3. Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps

      # 4. Verify Playwright installation
      - name: Verify Playwright Installation
        run: |
          playwright --version

      # 5. Run tests and generate JUnit XML report
      - name: Run tests
        run: |
          mkdir -p reports
          pytest --junitxml=reports/ui-report.xml

      # 6. Check if JUnit report was created
      - name: Check if JUnit report was created
        run: |
          ls -al reports

      # 7. Upload the test report as an artifact
      - name: Upload JUnit report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: junit-report
          path: reports/ui-report.xml
